"""SupChainMate REST API — a thin FastAPI layer over the existing
`modules/` and `ai/` packages. The API adds a JSON surface only; all
business logic and the AI router stay exactly where they are.
"""
